 int countSeniors(vector<string>& v) {
        int cnt = 0; 
        for(auto &x : v){
            int age = stoi(x.substr(11,2)) ;
            if(age > 60){
                cnt+=1 ;
            }
        }
        return cnt ;
        
    }
